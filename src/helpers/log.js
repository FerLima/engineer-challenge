    
    const setLog = (err) => {
        //tabela no banco com colunas:
        // createdat, message, status
        // salvar no banco de dados a partir do json
    }


    module.exports = {setLog }